# Volunteer JSON instruction implementation

Authorized: make existing admin ChatGPT JSON instruction available to volunteers.
Plan: extract shared taxonomy builder and prompt-dialog template; reuse unchanged prompt JS in volunteer add/edit; add a secondary ChatGPT · JSON button beside the existing guide. Keep import/publish permissions unchanged. Regression: add/edit expose prompt+taxonomy, other-place/admin access stays denied. Verify existing admin taxonomy tests, volunteer routes and local browser open/copy/reset/Escape/focus at five widths. No deploy/commit.

Completed: shared services/place_taxonomy_config.py (AST body matches former admin method exactly), shared json_prompt_dialog.html, volunteer add/edit button and same existing JS, prompt dialog outside form so editing instructions does not mark card dirty. Narrow mobile action wrapping; no permission changes.

Validation: initial test fixture lacked required category (corrected), then both add/edit regressions failed for missing instruction before implementation. After implementation36 tests passed (new2 + VolunteerAccessTests32 + taxonomy2); final new/taxonomy/admin-layout5 passed. Isolated runner /tmp/kidsmap-audit-20260908-qa/run.py with DJANGO_TESTING=1, in-memory SQLite, LocMem cache/email, blocked network. Exact final labels: catalog.testcases.test_volunteer_json_prompt catalog.testcases.place_taxonomy_admin catalog.testcases.admin.TestAdminOwnershipModerationUX.test_place_admin_change_form_uses_step_layout, flags --noinput --verbosity0 (CLI spelling: --verbosity 0).

Browser: node /tmp/kidsmap-audit-20260908-browser/volunteer-prompt.cjs, isolated local Chrome/server and synthetic volunteer, third-party requests blocked. Final15 route-prefix/width cases (ru/az/en ×390/768/1024/1280/1440) passed open/full prompt+taxonomy/copy clipboard/reset/Escape/focus return/no card dirty state/button containment; zero pageerrors. Initial screenshot revealed cropped footer button; targeted flex-wrap fixed and final matrix rerun. Final390 screenshot visually inspected. These are locale-prefixed routes, not a certification of complete translations. Own fixture server stopped after checks. No production/deploy/commit. git diff --check passed.
