# Validation A — PricingPlan impact

2026-09-08; execution identity: `/root/validate_a`. AUDIT; authorized change is this report only. Actual definitions read: `.agents/agents/kidsmap-orchestrator/agent.md`, then `.agents/agents/django-reviewer/agent.md`. One runtime agent performed routing and domain analysis sequentially; no independently executed support roles are claimed.

Snapshot: `git rev-parse HEAD` returned `d75b34f4db5d4ba484cf8392ff3ae0c9939031e2` (exit 0). Agent documents are concurrent dirty WORKTREE changes supplied by this task. Production version/data/schema: UNKNOWN, not contacted. No application modifications.

## Routing and order

Lead: **django-reviewer**, because the question changes a business model and its consumers. For this bounded impact report no support worker is necessary. If a concrete change follows: domain contract → database-reviewer for fields/constraints/data transitions → frontend-reviewer and seo-reviewer for changed public output → integration-reviewer; frontend-admin/browser-qa only when editor/rendered behavior changes. Security-reviewer joins only for altered permissions or exposed fields. These are conditional handoffs, not a request to launch the whole roster. Maximum three simultaneous specialists remains binding.

## Source-backed impact

| Boundary | Verified source | Impact of a change |
|---|---|---|
| Model validation and DB | `src/catalog/models/pricing_plan.py:PricingPlan`, `Meta`, `clean`, `save` | Field/enumeration changes may require schema migration plus updates to price-kind, billing, role, range/date/quantity constraints. `save` invokes `full_clean`; model-only work is insufficient. |
| Persistence/transport | `src/catalog/services/pricing_plans.py:PRICING_STORAGE_FIELDS`, `serialize_pricing_plan`, `normalize_pricing_plans`, `replace_place_pricing_plans` | A new field must survive allowlisting, normalization and serialization. Replacement is atomic, locks existing rows, matches ID/fingerprint, removes stale rows, preserves omitted verification. Changing fingerprint semantics affects ID continuity. |
| Scalar projections and legacy | Same service: `sync_legacy_price_fields`; model: `_sync_pricing_plan_legacy_fields`; `src/catalog/models/place.py:360`, `Place.save` | Save/delete signals recalculate AZN active-primary scalar prices. Empty remaining plans clear projections. Place property falls back to legacy JSON; compatibility is active source behavior, not deletion permission. Graph identifies both pricing migration commands; their runtime and full side effects were not inspected here. |
| Owner/admin/volunteer | `src/catalog/forms.py:1391,1469`; `src/catalog/domain_admin/place.py:233,268`; `src/catalog/volunteer_forms.py:94`; `src/catalog/services/volunteer_places.py:31,69,120,191` | All normalize or carry pricing payloads; volunteer snapshots/revisions also carry the contract. Draft, moderation submission, published-place compatibility and public visibility are separate gates. |
| Readiness divergence | `src/catalog/services/permanent_place_rules.py:36`; `src/catalog/services/place_readiness.py:239,252`; `src/catalog/services/content_quality.py:_has_pricing_plan_q`, `_place_has_pricing_plan_price` | Owner helper requires a public-price plan unless scalar legacy applies; canonical readiness exempts non-tariff modes. A change must test both paths and public SQL; current parity must not be assumed. This confirms an existing contract difference, not a newly reproduced runtime failure. |
| Import/export | `static/admin/js/kidsmap_place_json_import.js:399`; `src/catalog/domain_admin/place.py:validate_pricing_import_view`, `export_place_json_view` | JSON UI import receives server-normalized tariffs; export serializes relational plans. Include aliases, languages, decimals, missing fields, maximum 12 and re-import. CSV importer is a separate knowledge-listed consumer; not executed or fully audited. |
| API and public UI | `src/config/urls.py:31`; `src/catalog/views.py:place_pricing_api`; `src/catalog/templates/catalog/includes/place_card.html:38`; `src/catalog/templates/catalog/place_detail.html:189,337`; `Place.price_range_display` | `/api/v1/places/<slug>/pricing/` serializes active plans and summary; serializer additions can expose new fields publicly. Card/detail render backend price summaries. Map/filter/recommendation source wiring was completed in the post-feedback section below. |
| SEO | `src/catalog/services/seo.py:465` | Active primary exact/free/from/range plans become Offer/PriceSpecification with currency and localized title; `on_request` omitted here, free-mode fallback supported. Changes to price semantics require JSON-LD checks. |

Security: preserve staff-owned `verified_at` (`allow_verified=False` owner/volunteer versus staff admin), place-scoped replacement and API field exposure. Full route/object ACL audit was not performed; no exploit claimed. DB occupancy, deployed constraints and concurrent first-plan creation behavior are UNKNOWN.

## Evidence and discovery limits

Codebase Memory callable. `list_projects` selected root `/home/ramin/kidsmap`, project `home-ramin-kidsmap`; `index_status` ready. Architecture scoped to catalog; symbol search found `PricingPlan`. Inbound depth-2 trace returned 13 reachable callers across two pages (final page consumed): normalizers, replacement, Place.save, owner/admin/volunteer and migration commands. Call graph is discovery, not exhaustive runtime proof.

Coverage checked 21 exact application paths in three calls. All reported matching metadata; detail template is partial at 73, 92, 341, 343, 353–354, 531, 684. Relevant pricing range 333–358 was directly read. Other paths reported no recorded issue, which is not completeness proof. Watcher advanced generation from 05:29:58Z to 05:34:22Z during concurrent document work; no stale cursor error. No negative/dead-code claims made.

Tool counts: 10 Codebase Memory calls (list 1, status 2, architecture 1, search 1, trace 2, coverage 3); approximately 12 shell read/search calls before report creation. No graph cache artifacts written. Initial large mandatory-knowledge/status output was truncated; critical contract, lead, pricing and source conclusions were separately inspected. This run was less context-efficient than intended.

Read paths: orchestrator/django definitions; `.agents/README.md`, registry; rules engineering-contract/agent-orchestration/audit-contract; knowledge current-snapshot/source-of-truth/architecture/business-rules/deployment/testing/pricing/legacy/database. Application reads are the table's paths plus bounded searches of owner controller, repository, catalog URLs, catalog/home map JS and test files. `rg --files` inspected only test/migration/template filename lists. Missing guessed paths `knowledge/source-map.md`, `templates/includes/place_card.html`, `catalog/filters.py` were corrected or left unclaimed; these were operator navigation misses, not broken links in the actual role definition.

Prompt evaluation: routing and domain breadth worked; updated pricing knowledge prevented model-only impact and distinguished JSON import, scalar signals and volunteer revisions. Minor efficiency concern: long mandatory historical READ FIRST lists pull unrelated deployment/history into a narrow pricing question; prefer shared safety/current snapshot plus domain-specific sections. Engineering contract could explicitly recommend projecting `index_status` metadata to avoid large ignored-file lists. Historical pages retain contradictory-looking older claims, but dated banner/current snapshot gave an explicit precedence rule; no production claims were inherited.

## Verification plan and handoff

Executed: source/graph/coverage/HEAD reads only. Tests, Django checks, app/DB/browser/production, migrations/imports and external services: **NOT RUN**. No PASS assertion about application behavior.

After a concrete approved implementation and isolation of disposable PostgreSQL, LocMem cache, temporary media/email and disabled external integrations, integration-reviewer should run this targeted command (planned, not executed):

```sh
DJANGO_TESTING=1 python manage.py test catalog.testcases.pricing_plans_relational catalog.testcases.pricing_plans catalog.testcases.place_readiness catalog.testcases.legacy_migrations catalog.testcases.test_json_roundtrip_audit catalog.testcases.test_place_json_and_pricing_modes --noinput
```

`DJANGO_TESTING=1` alone does not prove DB/media/integration isolation. Existing test method names inspected in `pricing_plans_relational.py` include stable IDs/scalars, free/foreign/on-request cases, filters, API, idempotence, delete projections, owner verification and admin import/export; `test_json_roundtrip_audit.py` supplies representative mode round-trips. Add behavior-specific assertions only for actual gaps: new field round-trip, forged cross-place ID/verification, failure rollback and concurrency if touched. Schema changes additionally require isolated migration drift and migration/rollback verification. For affected UI: AZ/RU/EN at 390/768/1024/1280/1440, DOM/console/network, overflow and keyboard; no source read counts as browser evidence.

Next: **django-reviewer** receives the proposed PricingPlan delta and confirms exact affected allowlists/semantics; acceptance is a concrete scope mapping each changed field/rule to normalization, persistence, exposure and regression checks. **database-reviewer** then handles any schema/legacy change, with ambiguities marked manual_review. Until the delta exists, exact migration/backfill/rollback scope remains UNKNOWN. Stop at this impact report; no fixes authorized by it.

## Post-feedback bounded revalidation

Re-read the updated engineering contract: READ FIRST now explicitly means navigation, current source map path is stated, and index_status projection is required. Both previously suggested prompt improvements are addressed in the dirty WORKTREE definition. Exercised the projection: current status `ready`, root `/home/ramin/kidsmap`, 8309 nodes / 31336 edges, without echoing ignored-file inventory. This is post-feedback revalidation of those instructions; initial run inefficiency remains recorded above.

Additional source-backed consumers:

- **Catalog filtering/sorting:** `src/catalog/services/filtering.py:202–232`, `PlaceListFilters.apply`, queries PricingPlan directly: active primary AZN exact/from/range plans; positive effective price uses `price` for exact, otherwise `price_min`, taking the minimum per Place. Bounds and ascending/descending sorting use this subquery. Therefore scalar summary changes alone do not update this contract; new kinds/currency/primary semantics may change which cards survive filters. `src/catalog/controllers/place_controller.py:68–85` applies filters before both pagination and catalog map serialization.
- **Catalog cards and map payload:** `src/catalog/templates/catalog/place_list.html:852` includes the shared card, while `:696` serializes `catalog_map_places`. `PlaceController._serialize_map_places` (`:728,770`) sends `price=str(place.card_price_badge)`; `DjangoPlaceRepository.map_ready_queryset` (`src/catalog/repositories/django_repositories.py:68`) prefetches pricing and restricts coordinates. `static/js/catalog_map.js:renderMapCard` was directly read: its card markup renders image/name/category/rating/address/actions without a price element. Thus pricing affects payload and filtered map membership; this source does not justify claiming a visible catalog-map price badge. No rendered-browser conclusion.
- **Home map:** `src/catalog/controllers/home_controller.py:80` emits the same backend badge as `price`; `src/catalog/templates/pages/home.html:327` JSON-embeds map data and `:2771` loads home_map.js. `static/js/home_map.js:232–250`, `renderPopupContent`, reads `place.price` into the popup badge. This is a direct visible-price consumer in source.
- **Recommendations:** `DjangoPlaceRepository.top_popular` (`:61–66`) starts from the public-visible queryset, selects staff recommendations when present, otherwise popularity fallback. `HomeController.build_context` (`:43`) takes four and returns them as `popular_places`; `pages/home.html:331–336` renders shared place cards. Pricing changes can alter public eligibility and card price display. The verified selection order is recommendation order/update/likes, not a price ranking.

Follow-up added 5 MCP calls (three bounded symbol searches, one coverage, one projected status), approximately 7 read/search shell calls. Search results were not truncated; two unsuitable patterns returned zero and were followed by bounded filename/literal discovery, never treated as absence proof. Coverage added six exact source paths, all metadata-matched; home and catalog list templates have partial parsing, so actual pertinent JSON/script/include lines were inspected. Total: 15 MCP calls and 27 exact paths coverage-checked. No application/test/DB/browser commands executed. Remaining UNKNOWN is runtime behavior and proposed change/migration scope, not these established consumer links.
