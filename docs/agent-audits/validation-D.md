# Validation D — Place card impact

Date: 2026-09-08. Execution identity: runtime child `/root/validate_d`, fresh task context; actual canonical orchestrator and frontend definitions read from disk. This is independent validation of the updated role instructions, not native-agent discovery verification.

## Classification and dispatch

Input: «Какие компоненты затронет редизайн Place card?» Classification: bounded public UI REDESIGN **impact analysis**, AUDIT ONLY. One lead: `frontend-reviewer` (`.agents/agents/frontend-reviewer/agent.md`); routing owner: `kidsmap-orchestrator`. No implementation authorized by this question. This run starts no additional agents.

Minimal support for a subsequent concrete redesign: `seo-reviewer` for URLs/semantics, `analytics-reviewer` for CTA events, `browser-qa` for rendered acceptance. These are proposed handoffs, not three independently executed reviews. Consult `django-reviewer` only when displayed fields/formatters/payload semantics change; `frontend-admin` only if volunteer UI is explicitly included. `integration-reviewer` owns targeted regression selection before browser acceptance. No database/release worker needed for a template/style-only scope.

Order: establish backend display boundaries → frontend impact and concrete UI plan → user decision on implementation scope → approved UI changes with SEO/analytics boundary checks → targeted integration verification → rendered browser checks → orchestrator handoff. Existing document/read authorization persists.

## Snapshot and evidence method

- `git rev-parse HEAD`: `d75b34f4db5d4ba484cf8392ff3ae0c9939031e2`, exit 0.
- WORKTREE: agent-system changes already present. `git diff --stat` confirms definitions/knowledge/workflows/registry/report changes. `git diff --name-only -- src static templates scripts` and `git ls-files --others --exclude-standard src static templates scripts`: empty, exit 0. Application source read here agrees with LOCAL HEAD for these scopes.
- PRODUCTION: UNKNOWN, never contacted. September 6 deployment observations are historical.
- Codebase Memory first for structural discovery: `list_projects` → `index_status` → one `get_architecture` → narrowed `search_graph`/`trace_path` → exact coverage → source verification. Project `home-ramin-kidsmap`, root `/home/ramin/kidsmap`, ready, 8300 nodes/31327 edges at initial status.
- Exact coverage generation `2026-09-08T05:35:46Z`, mode `full`, generation_matches=true. All cited application paths checked with `check_index_coverage`; freshness metadata_match. Current `site.css`, `catalog_map.js`, `home_map.js`, motion/phone assets have **no_recorded_issue**. Static is NOT uniformly excluded in this run. This corrects the task's historical exclusion assumption; no index mutation performed here.
- Partial template coverage: home, catalog list, detail, base, phone include and volunteer card/index. Direct source reads verify the cited template boundaries. No recorded issue is not completeness proof.
- Broad `search_graph(query="place card", file_pattern="*catalog*", limit=12)` returned 932 matches/has_more; narrowed to `card_price_badge.*` in models/place.py: four results, has_more=false. Price property call trace returned zero CALLS; direct model source establishes the property delegation. `build_public_price_summary` inbound depth1 returned four callers, no remaining page: `Place.price_range_display`, `client_rules`, `get_starting_price`, `place_pricing_api`. Those extra callers are graph impact leads, not fully source-audited endpoints here.
- JS graph narrowing found `catalog_map.renderMapCard`, lines273–343, has_more=false. Broad template coverage scope had 41 gaps/has_more; deliberately narrowed to exact cited paths rather than claiming full template coverage. Index status exclusion listing was truncated; no exhaustive exclusion conclusion taken from it.

## Precise consumer map

| Boundary | Verified source | Impact |
|---|---|---|
| Shared public card | `src/catalog/templates/catalog/includes/place_card.html:3` | `.card.place-card`, `data-place-id`; photo/placeholder, category icon include, temporary badge, formatted price, favorite form/auth prompt, localized title/description/age/rating/location, detail link and phone reveal. The complete include currently has no schedule row. |
| Direct shared consumers | `src/catalog/templates/pages/home.html:336`, `src/catalog/templates/catalog/place_list.html:852`, `src/catalog/templates/pages/account_favorites.html:116` | Redesign propagates to recommended places, catalog results and account favorites. Literal include search over `src/catalog/templates` also separates the volunteer include. Favorites must not be omitted from acceptance. |
| Catalog dynamic replacement | `src/catalog/templates/catalog/place_list.html:1769`, `:1801`, `:1844`, `:1898` | Grid skeletons and `replaceCatalogFragment` replace `.catalog-top-stack`/`.catalog-main-column`, refresh map and rebind dynamic controls; verify cards after AJAX filter changes and history navigation, not only initial render. Search suggestion markup at `:2589` is separate `.search-card-*`, not this include. |
| CSS cascade | `static/css/site.css:4627`, `:4867`, `:6844`, `:31191`, `:31303`, `:31560`; `src/catalog/templates/base.html:51`, `:55`, `:97`; `static/css/motion.css:72`, `:215` | Global card styles, catalog list/grid variants, home recommended overrides and later `.card.place-card` rules combine with base inline overrides and late motion/phone CSS. Changing a selector or one early rule does not establish computed result. |
| Shared JS behavior | `src/catalog/templates/base.html:396`, `:578`; `static/js/motion.js:42`; phone include `:3` and `static/js/place_phone_reveal.js:252` | Preserve photo fallback attributes, delegated favorite submission, auth prompt and next URL, motion card ancestry and `data-phone-*` selectors. Phone include shared by card and detail is a real shared subcomponent even though whole markup differs. |
| Home map popup | `static/js/home_map.js:285`, `:294`, `:297`; `src/catalog/controllers/home_controller.py:58`, `:80`, `:83`; `static/css/site.css:7428` | Separate `.home-map-popup` string-rendered markup; receives server price/schedule and displays schedule; `kidsMapPhoneButton` for phone reveal. Does not consume the public card include. |
| Catalog map card | `static/js/catalog_map.js:273`, `:301`, `:529`, `:546`; `src/catalog/controllers/place_controller.py:728`, `:770`, `:778`; `static/css/site.css:7808` | Separate `.catalog-map-card-*`, desktop card/mobile sheet. Source renderer shows image/title/category/rating/address/detail/route/phone; price/schedule in payload do not imply those fields are rendered here. Different from both home popup and public include. |
| Full place detail | `src/catalog/templates/catalog/place_detail.html:189`, `:225`, `:257`, `:383`; `src/catalog/controllers/place_controller.py:844` | Separate decision panel, pricing, schedule, gallery/reviews/contact markup and own context. Shared price/contact changes can affect it indirectly; a public list-card layout change does not automatically redesign detail. |
| Volunteer card | `src/catalog/templates/admin/volunteer/place_card.html:2`, `:10`; index `:13`, detail `:7`, base `:3` in that directory | Separate `.kv-card`, `card.candidate`, state/readiness/proposed/public review data and edit/actions. Loaded by volunteer index/detail with `static/admin/css/pages/volunteer.css`. No assumption of public shared markup; outside public redesign unless scope expands to staff surface. |
| Backend formatting | `src/catalog/models/place.py:375`, `:386`, `:394`, `:398`, `:494`; `src/catalog/services/pricing_plans.py:461`, `src/catalog/services/place_schedule.py:565` | `card_price_badge` and value return the full `price_range_display` label; currency property returns empty. Do not split the label as numeric amount or append currency blindly. Model delegates localized price/schedule to backend services. No client-side copy of pricing/schedule business rules. |
| Public URL / SEO | `src/catalog/models/place.py:get_absolute_url`, `src/catalog/urls.py:162`, `:163`; `src/catalog/templates/base.html:18`; `src/catalog/services/seo.py:359`, `:415`, `:466` | Preserve real localized detail links, title/image semantics; canonical/hreflang remain shell responsibilities. Detail SEO builds LocalBusiness hours/Offer data independently. Changing shared formatter/schema data requires a consistency review; CSS alone does not change JSON-LD. |
| Analytics | `src/catalog/templates/base.html:710`, `:760`; `static/js/place_phone_reveal.js:138`, `:163`, `:179`; `src/catalog/controllers/place_controller.py:847`; `src/catalog/services/tracking.py:226` | Phone reveal creates `cta_call`/`cta_whatsapp` targets and place/source attributes; base delegates to tracking endpoint. Detail entry records place_open server-side. Preserve these behaviors and avoid inventing a card-click event that the include does not currently declare. Detail map/Instagram CTA attributes are separate consumers (`place_detail.html:270`, `:288`). |

## Planned verification, not executed

After approval and an isolated local app: AZ/RU/EN; widths390/768/1024/1280/1440, plus320/360 for substantial redesign. Actual DOM/computed style, console/network/static404, horizontal overflow, keyboard/focus/touch targets and reduced motion. Check long/empty names and descriptions, photo present/missing/failure, free/from/on-request price text, empty/populated ratings, adult/temporary badges, guest/authenticated favorites, phone loading/error/reveal/call, initial and AJAX catalog grid/list, favorites and home. Check maps/detail only to the extent shared assets/formatters/phone subcomponent affect them; do not claim a blanket redesign.

Targeted candidate command after explicit test isolation is established: `DJANGO_TESTING=1 .venv/bin/python manage.py test catalog.testcases.public catalog.testcases.catalog catalog.testcases.phone_reveal catalog.testcases.tracking --noinput`. This is PLANNED, not invoked. Use disposable DB/cache/media/email and disabled external integrations; no inherited production credentials. Expand pricing/schedule/SEO tests only for actual corresponding contract changes. Record exact snapshot, counts, failures and baseline attribution; never alter assertions for green. No app test, browser, DB, production or external analytics action occurred in D.

## Role/prompt validation and defects

The updated role successfully routes this to frontend impact analysis, requires template→CSS→JS→backend→consumer evidence, distinguishes map/detail markup and forbids autonomous fixes. Compact handoff fits this task; no manufactured application finding or nine-section full audit is needed.

Remaining instruction/document defects: (1) the validation assignment assumes static exclusions, contradicted by current coverage; use runtime evidence. (2) old public-ui knowledge and routing table still mention smaller/historical viewport matrices, although current engineering contract explicitly supersedes them. (3) role mentions home/catalog shared include but not favorites; treat that as examples, never an exhaustive list. (4) overlapping READ FIRST lists can cause repeated broad document reads; source-of-truth.md is the actual map, not a file named source-map.md. An initial attempted read of the latter failed and was corrected. Broad initial graph/status and literal output was excessive/truncated; narrow and project structured coverage fields earlier in future runs.

Native loading: UNKNOWN in this session. Registry/README claim new-session standalone TOML discovery; runtime definition read/injection is demonstrated by this child reading the actual Markdown, not evidence that a fresh Codex session auto-loaded adapters. No native/session test performed. No subagents launched by D. UI skills kidsmap-ui-design and frontend-design read for future acceptance; this run makes no visual quality claim. Verification-before-completion read for report checks.

## Compact handoff

- Task/mode/authorization/snapshot: bounded Place card impact, AUDIT, report-only, LOCAL HEAD above plus agent-system dirty WORKTREE; production UNKNOWN.
- Changed: only `docs/agent-audits/validation-D.md` by this worker.
- Evidence: graph/coverage and bounded source map above; no graph caches, private records or secrets copied.
- Business impact: preserve backend price/schedule and visibility contracts; direct shared include has three confirmed consumer templates, while other Place surfaces use different markup.
- DB impact: none for proposed layout-only scope; existing favorite/tracking/phone endpoints must retain behavior.
- Security impact: preserve escaping, CSRF/auth/next behavior and phone reveal; no raw phone exposure in markup/payload redesign.
- Tests executed/not run: source/graph/git/document checks only; all application/runtime tests NOT RUN by task constraint.
- UNKNOWN/manual_review/risks: actual visual defects, production behavior, external Maps/GA, native loading and exact redesign scope unknown. No confirmed P0/P1 application finding; evidence is an impact map.
- Needs next: `kidsmap-orchestrator` confirms whether redesign scope is shared list card only or includes separate popup/detail/staff surfaces; acceptance is a concrete file/state plan. `frontend-reviewer` then owns that plan, reusing cited boundaries; no application changes before authorized implementation scope.

## Post-feedback retest — 2026-09-08

Re-read the updated frontend role, engineering contract and current snapshot; read bounded changed sections of public/admin UI knowledge and routing. This is a document/source-evidence consistency retest using the source observations above, not another application check or a new independent runtime launch.

- **Resolved:** frontend operating rules explicitly include `account_favorites`, distinguish volunteer markup and call the consumer list non-exhaustive. This agrees with the initial direct include search (home336/catalog852/favorites116, separate volunteer index/detail include).
- **Resolved:** current-snapshot and public-ui record no schedule row in the shared card, full backend price label and empty currency property; these agree with the full include read and `Place` properties375/386/394/398. No numeric/currency split or schedule-row assumption is introduced.
- **Resolved:** active public routing row and public-ui browser matrix now use390/768/1024/1280/1440; engineering contract still adds320/360 for substantial redesign. The inspected admin-ui knowledge has mobile/tablet acceptance without a competing numerical matrix. Historical rules are not a competing acceptance matrix.
- **Resolved:** engineering contract now explicitly treats READ FIRST as navigation, identifies `.agents/knowledge/source-of-truth.md`, requires runtime coverage checks and compact projection of large tool output. Public-ui explicitly notes that the current index can cover static. The initial run's overreading, mistaken source-map filename attempt and excessive/truncated output remain recorded execution limitations; document edits cannot retroactively make that run context-efficient.
- **Remaining qualification:** current-snapshot's runtime paragraph still summarizes earlier static/docs exclusions without individual path coverage. Its watcher/freshness instruction plus the engineering contract and corrected public-ui paragraph require rechecking per path; initial D coverage remains the actual static evidence for this report.

Result: the concrete D role/knowledge feedback is addressed at document level. Native discovery, rendered behavior, production and all application tests remain NOT VERIFIED/NOT RUN. This retest changes only D; no application, browser, DB or production checks were run.
